spring.application.name=control
spring.application.prefix=control
info.build.artifact=@project.artifactId@
info.build.name=@project.name@
info.build.description=@project.description@
info.build.version=@project.version@

#DB CONFIG
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.sql.init.platform=postgres
spring.datasource.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.jdbcUrl=${spring.datasource.url}
spring.datasource.url=jdbc:postgresql://localhost:5432/abacox-control?useUnicode=yes&characterEncoding=UTF-8
spring.datasource.username=postgres
spring.datasource.password=betax964566

spring.cloud.gateway.discovery.locator.enabled=false

#HTTP config
server.port=6060
server.forward-headers-strategy=framework

#Language config
spring.messages.fallback-to-system-locale=false
spring.web.locale=en
spring.web.locale-resolver=fixed

#Logging config
logging.file.name=logs/${spring.application.name}.log
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=200

#Swagger config
springdoc.swagger-ui.operationsSorter=alpha
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.doc-expansion=none
springdoc.remove-broken-reference-definitions=false
springdoc.model-and-view-allowed=true
springdoc.swagger-ui.server-url=

abacox.internal-api-key=441e5d28-dea8-49ce-ad7d-da50f830cb2f
abacox.path-prefix=